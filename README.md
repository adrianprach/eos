# EOS
Boot.dev agent tutorial

## Installation and run
- `uv sync`
- create `.env` and set `GEMINI_API_KEY` from api studiokey
- `uv run main.py "prompt goes here"`


## Supported functions:
- List files and directories
- Read file contents
- Execute Python files with optional arguments
- Write or overwrite files
